<Window x:Class="todolist_mvvm.view.Addtaskwindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:todolist_mvvm.view"
        mc:Ignorable="d"
        Icon="/Assets/Images/edit.jpg"
        Title="Add New Task" Height="400" Width="600" MinHeight="400" MinWidth="400">
    <Grid Background="{StaticResource bgimage}">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <TextBox Name="Taskname"
                 Grid.Row="1"
                 Grid.ColumnSpan="3"
                 TextWrapping="Wrap"
                 Style="{StaticResource CustomTextBox}"
                 Background="{StaticResource addtaskwatermark}"
                 TextChanged="Taskname_TextChanged"/>
        <TextBox Name="Add_descripton"
                 Grid.Row="2"
                 Grid.ColumnSpan="3"
                 Style="{StaticResource CustomTextBox}"
                 Height="Auto"
                 TextWrapping="Wrap"
                 MinHeight="80"
                 Background="{StaticResource Descriptionwatermark}"
                 TextChanged="Add_descripton_TextChanged"/>
        <Button Name="addtaskbutton"
                Style="{StaticResource Rounded3DButtonStyle}"
                Click="addtaskbutton_Click"
                Content="Add"
                Height="30" 
                Width="60"
                FontSize="12"
                FontWeight="DemiBold"
                Margin="20,10,30,0" 
                Grid.Row="3"
                Grid.Column="1"/>

    </Grid>
</Window>
