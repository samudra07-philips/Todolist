<Page x:Class="todolist_mvvm.view.LoginPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      mc:Ignorable="d"
      Title="Login Page">

    <Grid>
  
        <Grid.RowDefinitions>
            <RowDefinition Height="2*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="2*" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <TextBox Name="textboxsample" 
                 Grid.Row="1" 
                 Margin="5"
                 Text="Enter your name"
                 GotFocus="RemoveText" 
                 LostFocus="AddText"
                 TextChanged="textboxsample_TextChanged" Grid.ColumnSpan="4" Width="320" />

        <TextBox Name="passboxsample" 
                 Grid.Row="2"
                 Grid.ColumnSpan="4"
                 Margin="5"
                 Text="Enter your password" 
                 GotFocus="RemoveText" 
                 LostFocus="AddText"
                 TextChanged="passboxsample_TextChanged" Width="320"/>

        <Button Name="SignUp" 
                Content="Sign Up"
                Grid.Row="3" Grid.Column="1"
                Margin="20,10,30,0" 
                Click="Signedup" />

        <Button Name="Login"
                Content="Login"
                Grid.Row="3" Grid.Column="2"
                Margin="30,10,20,0"
                Click="Logedin" />
    </Grid>
</Page>